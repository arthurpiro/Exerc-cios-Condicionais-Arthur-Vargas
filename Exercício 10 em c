#include <stdio.h>

int main() {
    int valor;
 
    for (valor = 0; valor <= 5; valor += 2) {
        valor = valor * 2;
        printf("seu valor ao quadrado È: %d\n", valor);
    }

    return 0;
}
