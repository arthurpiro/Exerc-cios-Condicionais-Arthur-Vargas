#include <stdio.h>

int main() {
    int num, numero;

    printf("insira um numero: ");
    scanf("%d", &num);

    for (num = 0; numero <= num; num = +2) {
        printf("%d\n", num);
    }

    return 0;
}
